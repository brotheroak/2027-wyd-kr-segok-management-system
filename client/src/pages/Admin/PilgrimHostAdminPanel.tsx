import React, { useEffect, useRef, useState } from "react";
import { Camera, HeartPulse, Link2, Printer, ScanBarcode, Search, Trash2, Utensils } from "lucide-react";
import { api } from "../../api.js";
import type { Pilgrim } from "../../types.js";

type Host = { id: string; applicationNo: string; name: string; address: string; capacity: number };
type PilgrimForm = Omit<Pilgrim, "id" | "host" | "mealLogs" | "createdAt" | "updatedAt">;
const emptyPilgrim = (): PilgrimForm => ({ pilgrimNo: "", name: "", gender: "남성", diocese: "", region: "", grade: "", age: 18, dietType: "일반식", dietNotes: "", allergies: "", healthNotes: "", feverStatus: "정상", hostApplicationId: "" });

function BarcodeCard({ pilgrim }: { pilgrim: Pilgrim }) {
  const ref = useRef<SVGSVGElement>(null);
  useEffect(() => {
    let active = true;
    import("jsbarcode").then(({ default: JsBarcode }) => {
      if (active && ref.current) JsBarcode(ref.current, pilgrim.pilgrimNo, { format: "CODE128", width: 2, height: 58, displayValue: true, margin: 8 });
    });
    return () => { active = false; };
  }, [pilgrim.pilgrimNo]);
  return <div className="pilgrim-barcode-card" id={`barcode-${pilgrim.id}`}><strong>2027 WYD SEOUL · SEGOK</strong><h3>{pilgrim.name}</h3><svg ref={ref} /><span>{pilgrim.diocese} · {pilgrim.region}</span></div>;
}

export function PilgrimHostAdminPanel({ token, canViewPersonalData }: { token: string; canViewPersonalData: boolean }) {
  const [pilgrims, setPilgrims] = useState<Pilgrim[]>([]); const [hosts, setHosts] = useState<Host[]>([]);
  const [form, setForm] = useState<PilgrimForm>(emptyPilgrim()); const [editingId, setEditingId] = useState<string | null>(null);
  const [selected, setSelected] = useState<Pilgrim | null>(null); const [query, setQuery] = useState(""); const [scanValue, setScanValue] = useState(""); const [cameraOpen, setCameraOpen] = useState(false); const [message, setMessage] = useState("");
  const videoRef = useRef<HTMLVideoElement>(null);
  const load = () => api<{ pilgrims: Pilgrim[]; hosts: Host[] }>(`/api/admin/pilgrims?q=${encodeURIComponent(query)}`, {}, token).then((data) => { setPilgrims(data.pilgrims); setHosts(data.hosts); if (selected) setSelected(data.pilgrims.find((item) => item.id === selected.id) ?? null); });
  useEffect(() => { if (canViewPersonalData) load().catch((error) => setMessage((error as Error).message)); }, [token, canViewPersonalData]);
  useEffect(() => {
    if (!cameraOpen || !videoRef.current) return;
    let active = true; let controls: { stop: () => void } | undefined;
    import("@zxing/browser").then(({ BrowserMultiFormatReader }) => {
      if (!active || !videoRef.current) return;
      const reader = new BrowserMultiFormatReader();
      return reader.decodeFromVideoDevice(undefined, videoRef.current, (result) => { if (!result) return; const value = result.getText(); setScanValue(value); const found = pilgrims.find((item) => item.pilgrimNo === value); if (found) { setSelected(found); setCameraOpen(false); } }).then((next) => { controls = next; });
    }).catch(() => { if (active) setMessage("카메라를 시작하지 못했습니다. 브라우저 권한을 확인해 주세요."); });
    return () => { active = false; controls?.stop(); };
  }, [cameraOpen, pilgrims]);
  if (!canViewPersonalData) return <section className="admin-feature-panel locked-feature"><HeartPulse /><h2>순례자 건강·식단 정보는 개인정보 관리자만 볼 수 있습니다.</h2><p>건강정보는 민감정보로 분류되어 일반 운영자 화면에서는 가명 처리됩니다.</p></section>;
  const submit = async (event: React.FormEvent) => { event.preventDefault(); try { await api(editingId ? `/api/admin/pilgrims/${editingId}` : "/api/admin/pilgrims", { method: editingId ? "PUT" : "POST", body: JSON.stringify(form) }, token); setForm(emptyPilgrim()); setEditingId(null); setMessage("순례자 정보를 저장했습니다."); await load(); } catch (error) { setMessage((error as Error).message); } };
  const identify = () => { const found = pilgrims.find((item) => item.pilgrimNo.toLowerCase() === scanValue.trim().toLowerCase()); if (found) setSelected(found); else setMessage("일치하는 순례자 ID가 없습니다."); };
  const edit = (item: Pilgrim) => { setEditingId(item.id); setForm({ pilgrimNo: item.pilgrimNo, name: item.name, gender: item.gender, diocese: item.diocese, region: item.region, grade: item.grade, age: item.age, dietType: item.dietType, dietNotes: item.dietNotes, allergies: item.allergies, healthNotes: item.healthNotes, feverStatus: item.feverStatus, hostApplicationId: item.hostApplicationId ?? "" }); window.scrollTo({ top: 0, behavior: "smooth" }); };
  const printBarcode = (item: Pilgrim) => { const content = document.getElementById(`barcode-${item.id}`)?.outerHTML; if (!content) return; const win = window.open("", "_blank", "width=500,height=500"); win?.document.write(`<html><head><title>${item.pilgrimNo}</title><style>body{font-family:sans-serif;display:grid;place-items:center;padding:40px}.pilgrim-barcode-card{text-align:center;border:1px solid #bbb;padding:24px}.pilgrim-barcode-card>*{display:block;margin:8px auto}</style></head><body>${content}<script>window.onload=()=>window.print()<\/script></body></html>`); win?.document.close(); };
  return <section className="admin-feature-panel pilgrim-admin"><header><div><span>Pilgrim & host</span><h2>순례자 및 호스트 매핑</h2><p>순례자 ID, 배정 가정, 식단·알레르기·발열 정보를 한 화면에서 관리합니다.</p></div></header><div className="scanner-strip"><label><ScanBarcode /><input value={scanValue} onChange={(e) => setScanValue(e.target.value)} onKeyDown={(e) => e.key === "Enter" && identify()} placeholder="순례자 ID 스캔 또는 입력" /></label><button className="primary" onClick={identify}><Search size={17} /> 조회</button><button className="secondary" onClick={() => setCameraOpen(!cameraOpen)}><Camera size={17} /> 카메라 스캔</button></div>{cameraOpen && <div className="camera-reader"><video ref={videoRef} muted playsInline /><button className="modal-close-button" onClick={() => setCameraOpen(false)}>닫기</button></div>}{message && <p className="form-message">{message}</p>}<div className="admin-feature-grid"><form className="feature-form" onSubmit={submit}><h3>{editingId ? "순례자 정보 수정" : "순례자 등록"}</h3><div className="form-grid two"><label><span>순례자 ID (자동 생성 가능)</span><input value={form.pilgrimNo} onChange={(e) => setForm({ ...form, pilgrimNo: e.target.value })} /></label><label><span>성명</span><input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></label><label><span>성별</span><select value={form.gender} onChange={(e) => setForm({ ...form, gender: e.target.value })}><option>남성</option><option>여성</option></select></label><label><span>나이</span><input type="number" min={10} max={100} value={form.age} onChange={(e) => setForm({ ...form, age: Number(e.target.value) })} /></label><label><span>교구</span><input required value={form.diocese} onChange={(e) => setForm({ ...form, diocese: e.target.value })} /></label><label><span>지역</span><input required value={form.region} onChange={(e) => setForm({ ...form, region: e.target.value })} /></label><label><span>학년</span><input required value={form.grade} onChange={(e) => setForm({ ...form, grade: e.target.value })} /></label><label><span>식단</span><select value={form.dietType} onChange={(e) => setForm({ ...form, dietType: e.target.value })}>{["일반식","페스코","락토오보","락토","오보","비건","육류 제외","기타"].map((v) => <option key={v}>{v}</option>)}</select></label></div><label><span>배정 호스트</span><select value={form.hostApplicationId} onChange={(e) => setForm({ ...form, hostApplicationId: e.target.value })}><option value="">미배정</option>{hosts.map((host) => <option key={host.id} value={host.id}>{host.applicationNo} · {host.name} ({host.capacity}명)</option>)}</select></label><label><span>식단 상세</span><input value={form.dietNotes} onChange={(e) => setForm({ ...form, dietNotes: e.target.value })} /></label><label><span>알레르기</span><input value={form.allergies} onChange={(e) => setForm({ ...form, allergies: e.target.value })} /></label><label><span>건강 및 기타</span><textarea value={form.healthNotes} onChange={(e) => setForm({ ...form, healthNotes: e.target.value })} /></label><label><span>발열 상태</span><select value={form.feverStatus} onChange={(e) => setForm({ ...form, feverStatus: e.target.value })}>{["정상","관찰","발열","진료 필요"].map((v) => <option key={v}>{v}</option>)}</select></label><div className="button-row"><button className="primary" type="submit">{editingId ? "수정 저장" : "순례자 등록"}</button>{editingId && <button className="secondary" type="button" onClick={() => { setEditingId(null); setForm(emptyPilgrim()); }}>취소</button>}</div></form><div><div className="pilgrim-list-tools"><label className="community-search"><Search /><input value={query} onChange={(e) => setQuery(e.target.value)} onKeyDown={(e) => e.key === "Enter" && load()} placeholder="ID, 성명, 교구, 지역, 식단 검색" /></label><button className="secondary" onClick={() => load()}>검색</button></div><div className="pilgrim-list">{pilgrims.map((item) => <article key={item.id} onClick={() => setSelected(item)}><div><span className={`health-dot ${item.feverStatus !== "정상" ? "warning" : ""}`} /><strong>{item.name}</strong><small>{item.pilgrimNo}</small></div><dl><span>{item.gender} · 만 {item.age}세</span><span>{item.diocese} / {item.region} / {item.grade}</span><span><Utensils /> {item.dietType}{item.allergies ? ` · 알레르기: ${item.allergies}` : ""}</span><span><Link2 /> {item.host ? `${item.host.name} (${item.host.applicationNo})` : "호스트 미배정"}</span></dl><div className="icon-actions"><button onClick={(e) => { e.stopPropagation(); edit(item); }}>수정</button><button title="바코드 출력" onClick={(e) => { e.stopPropagation(); printBarcode(item); }}><Printer size={17} /></button><button title="삭제" onClick={async (e) => { e.stopPropagation(); if (!confirm("순례자 정보를 삭제하시겠습니까?")) return; await api(`/api/admin/pilgrims/${item.id}`, { method: "DELETE" }, token); await load(); }}><Trash2 size={17} /></button></div><div className="print-only"><BarcodeCard pilgrim={item} /></div></article>)}{!pilgrims.length && <p className="empty-copy">등록된 순례자가 없습니다.</p>}</div></div></div>{selected && <div className="fixed inset-0 z-50 bg-catholic-navy/70 p-4 flex items-center justify-center" onClick={() => setSelected(null)}><div className="pilgrim-detail-modal" onClick={(e) => e.stopPropagation()}><header><div><span>{selected.pilgrimNo}</span><h2>{selected.name} 순례자 카드</h2></div><button className="modal-close-button" onClick={() => setSelected(null)}>닫기</button></header><BarcodeCard pilgrim={selected} /><div className="pilgrim-alerts"><div><Utensils /><span>식단</span><strong>{selected.dietType}</strong><p>{selected.dietNotes || "추가 요청 없음"}</p></div><div><HeartPulse /><span>알레르기·건강</span><strong>{selected.feverStatus}</strong><p>{selected.allergies || "알레르기 없음"} / {selected.healthNotes || "특이사항 없음"}</p></div><div><Link2 /><span>호스트</span><strong>{selected.host?.name || "미배정"}</strong><p>{selected.host?.address || "호스트를 배정해 주세요."}</p></div></div><div className="meal-actions"><span>식사 제공 기록</span>{["아침","점심","저녁","간식"].map((meal) => <button key={meal} className="secondary" onClick={async () => { await api(`/api/admin/pilgrims/${selected.id}/meals`, { method: "POST", body: JSON.stringify({ mealType: meal, note: `${selected.dietType} 기준 제공` }) }, token); await load(); }}>{meal} 제공</button>)}</div><ul className="meal-log-list">{selected.mealLogs?.map((log) => <li key={log.id}>{new Date(log.recordedAt).toLocaleString("ko-KR")} · {log.mealType} · {log.note}</li>)}</ul></div></div>}</section>;
}
