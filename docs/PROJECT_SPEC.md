# 프로젝트 사양서

## 1. 목적

2027 서울 WYD 세곡동 성당 접수 시스템은 본당 공동체가 순례자를 맞이하기 위해 필요한 홈스테이 호스트 신청, 자원봉사자 신청, 접수 확인, 운영자 검토 업무를 하나의 웹 시스템에서 처리하기 위한 프로젝트입니다.

## 2. 운영 기준 정보

| 항목 | 값 |
| --- | --- |
| 서비스명 | 2027 서울 WYD 세곡동 성당 홈스테이 / 자원봉사 신청 |
| 운영 주체 | 천주교 서울대교구 세곡동성당 WYD 본당 위원회 |
| 본당 주소 | 서울특별시 강남구 헌릉로618길 34 세곡동성당 |
| 대표 번호 | 02-459-8211 |
| WYD 예정 기간 표기 | 2027.08.01 - 2027.08.09 |
| 개인정보처리방침 시행일 | 2026.07.04 |

## 3. 사용자 유형

| 사용자 | 권한과 목적 |
| --- | --- |
| 신청자 | 홈스테이 호스트 신청서를 작성하고 접수 내역을 확인, 수정, 취소합니다. |
| 자원봉사자 | 자원봉사자 신청서를 작성하고 접수 결과 화면을 확인합니다. |
| 일반 운영자 `admin` | 신청 통계, 카드, 상태, 매칭 후보를 확인합니다. 주요 개인정보는 마스킹됩니다. |
| 개인정보 관리자 `privacy_admin` | 원본 개인정보 조회, 신청서 수정, 감사 로그 다운로드가 가능합니다. |
| 최고 관리자 `super_admin` | 일반 운영자/개인정보 관리자 승인, 권한 변경, 계정 상태 변경을 수행합니다. |

## 4. 신청자 화면 사양

### 신청 첫 화면

- URL: `/apply`
- 상단 브랜드 클릭 시 항상 `/apply`로 이동합니다.
- 신청 유형은 두 카드로 분리합니다.
  - 홈스테이 신청: 하트 아이콘
  - 자원봉사자 신청: 십자가 아이콘
- 카드 제목은 다음 두 줄 정렬을 유지합니다.
  - `2027 서울 WYD`
  - `홈스테이 신청` 또는 `자원봉사자 신청`

### 홈스테이 신청

- URL: `/apply/homestay`
- 주요 단계:
  - 서약 확인 및 동의
  - 신청자 인적사항
  - 가족 구성
  - 홈스테이 여건
  - 서명 및 완료
- 신청자는 4자리 숫자 PIN을 등록합니다.
- 접수 후 `/check`에서 성명, 연락처, 4자리 PIN으로 조회합니다.
- 주소는 카카오 우편번호 서비스로 검색하고 우편번호, 기본 주소, 상세 주소를 분리 저장합니다.

### 자원봉사자 신청

- URL: `/apply/volunteer`
- 인적사항:
  - 성명, 세례명, 성별, 생년월일, 연락처, 이메일, 주소
  - 생년월일 입력 시 만 나이를 자동 계산합니다.
- 지원 분야:
  - 행사 진행 및 안내
  - 통역 및 언어 지원
  - 의료 봉사
  - 중복 선택 가능
- 통역 및 언어 지원 선택 시 지원 언어를 복수 선택할 수 있습니다.
  - 기본 언어: English, Espanol, Francais, Italiano, Portugues, 日本語, 中文
  - 기타 선택 시 직접 입력란을 노출합니다.
- 활동 가능 시간:
  - 주간
  - 야간
  - 주간,야간 관계 없음
- 신청일은 자동 입력되며 수정하지 않습니다.
- 서명란은 성명과 세례명을 자동 매핑해 다음 양식으로 표시합니다.
  - `202X년 XX월 XX일`
  - `신청인 성명(세례명) : xxx (세례명) (서명 / 인)`

## 5. 운영자 화면 사양

### 공통

- URL: `/admin`
- 관리자 로그인은 이메일과 비밀번호를 기본으로 사용합니다.
- 가명/마스킹 정보만 보는 일반 운영자 `admin`은 MFA가 필수는 아닙니다.
- 원본 개인정보에 접근 가능한 `privacy_admin`, `super_admin`은 TOTP MFA가 필수입니다.
- MFA 필수 계정이 최초 MFA 미등록 상태이면 로그인 과정에서 `mfa_secret`을 안내하고 OTP 앱 등록 후 6자리 코드를 검증합니다.
- 운영자 회원가입, 비밀번호 변경, 최고 관리자 승인 워크플로우를 제공합니다.
- `brotheroak@gmail.com`, `livelab21@nate.com`은 고정 최고 관리자이며 다른 운영자가 권한을 변경할 수 없습니다.
- 홈스테이와 자원봉사자는 탭으로 구분합니다.
- 통합 검색은 접수번호뿐 아니라 성명, 연락처, 이메일을 지원합니다.

### 홈스테이 관리

- 접수 상태별 통계와 수용 가능 인원을 보여줍니다.
- 신청 카드에서 상태를 변경할 수 있습니다.
- 일반 운영자는 개인정보가 마스킹된 값을 봅니다.
- 개인정보 관리자는 원본 상세 조회 및 수정이 가능합니다.
- 순례자 조건 기반 매칭 후보를 조회할 수 있습니다.

### 자원봉사자 관리

- 총 신청, 승인 대기, 통역 지원 가능, 의료 봉사 가능 지표를 표시합니다.
- 소통 언어 분포 차트를 표시합니다.
- 지원 유형 비율 차트를 표시합니다.
- 샘플 데이터 생성 버튼으로 관리자 화면을 테스트할 수 있습니다.
- 신청 카드에서 상태를 변경할 수 있습니다.

## 6. 개인정보 및 보안 사양

- 운영 모드에서 `DATA_ENCRYPTION_KEY`가 없으면 서버가 시작되지 않습니다.
- 주요 개인정보 컬럼은 AES-256-GCM으로 암호화합니다.
- 신청자 PIN은 신청 ID와 조합한 SHA-256 해시로 저장합니다.
- 인증번호 조회용 이메일은 평문 대신 안정 해시를 사용합니다.
- 일반 운영자 응답에는 개인정보를 마스킹합니다.
- 개인정보 관리자 원본 조회, 수정, 감사 로그 다운로드는 `audit_logs`에 기록합니다.
- `/api`는 IP 기준 rate limit을 적용합니다.
- 동시 처리 요청은 `MAX_CONCURRENT_REQUESTS`로 제한합니다.

## 7. 비기능 요구사항

| 항목 | 기준 |
| --- | --- |
| 예상 최대 동시접속 | 100명 |
| 내부 웹 퍼널 기본값 | `MAX_CONCURRENT_REQUESTS=120` |
| 권장 운영 제한값 | 100명 기준 Cloud Run `max-instances=3`, `concurrency=50`, 앱 `MAX_CONCURRENT_REQUESTS=120`, `RATE_LIMIT_MAX=120`부터 시작 |
| 현재 운영안 | Google Cloud Run + Cloud SQL PostgreSQL |
| 로컬/단일 VM 대안 | SQLite + 파일 백업 |
| 브라우저 지원 | 최신 Chrome, Edge, Safari, 모바일 브라우저 |
| 첨부파일 | 현재 범위 제외 |
| 결제 | 현재 범위 제외 |

## 8. API 요약

| 메서드 | 경로 | 목적 |
| --- | --- | --- |
| GET | `/api/health` | 서버 생존 확인 |
| GET | `/api/ready` | DB 연결 readiness 확인 |
| GET | `/api/funnel/status` | 웹 퍼널 상태 확인 |
| POST | `/api/applications` | 홈스테이 신청 생성 |
| POST | `/api/applications/lookup` | 홈스테이 신청 조회 |
| POST | `/api/volunteers` | 자원봉사자 신청 생성 |
| POST | `/api/auth/request-code` | 이메일/SMS 인증번호 요청 |
| POST | `/api/auth/verify-code` | 인증번호 검증 |
| POST | `/api/admin/login` | 관리자 로그인 및 필요 시 MFA 검증 |
| POST | `/api/admin/register` | 운영자 회원가입 요청 |
| POST | `/api/admin/change-password` | 로그인한 운영자 비밀번호 변경 |
| GET | `/api/admin/users` | 최고 관리자 운영자 목록 조회 |
| PATCH | `/api/admin/users/:id` | 최고 관리자 운영자 승인/권한/상태 변경 |
| GET | `/api/admin/applications` | 홈스테이 관리자 목록 |
| GET | `/api/admin/volunteers` | 자원봉사자 관리자 목록 |
| GET | `/api/admin/audit-logs.csv` | 감사 로그 CSV 다운로드 |

## 9. 범위 밖 항목

- 실명 인증, 휴대폰 본인확인
- 순례자 개인별 배정 확정 워크플로우
- 파일 첨부 및 이미지 업로드
- SMS 벤더별 전용 SDK의 고급 재시도/장애 처리
- SQLCipher 같은 DB 파일 자체 암호화

이 항목들은 운영 요구가 확정되면 별도 phase로 정의합니다.
