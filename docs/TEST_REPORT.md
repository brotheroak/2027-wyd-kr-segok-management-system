# 테스트 리포트

최종 갱신: 2026-08-17 KST

## 1. 테스트 범위

이번 점검은 문서 현행화와 함께 현재 코드 기준의 기본 전수 테스트를 다시 수행하기 위한 기준 문서입니다.

| 분류 | 명령 또는 확인 항목 | 목적 |
| --- | --- | --- |
| 타입 검사 | `npm run typecheck` | 클라이언트/서버 TypeScript 타입 검증 |
| 단위 테스트 | `npm test` | 암호화, 비밀번호, TOTP, 입력 검증, 구역반 매핑 검증 |
| 스크립트 문법 | `node --check scripts/setup-db.mjs`, `node --check scripts/create-admin.mjs` | 운영 스크립트 문법 검증 |
| 프로덕션 빌드 | `npm run build` | 서버 컴파일 및 Vite 정적 빌드 |
| 헬스 체크 | `/api/health`, `/api/ready`, `/api/funnel/status` | 로컬 서버 상태 및 readiness 확인 |
| 운영 readiness | Cloud Run `/api/ready` | 운영 DB 연결 및 암호화 활성화 확인 |
| 구역반 API | `/api/district/assign` | 신규 구역표 기준 자동 매핑 회귀 확인 |
| 경량 부하 | `npm run stress` | rate limit/웹퍼널 전 정상 응답 확인 |

## 2. 2026-07-28 실행 결과

아래 결과는 이번 문서 현행화 후 실제 명령 실행 결과로 갱신합니다.

| 항목 | 결과 | 비고 |
| --- | --- | --- |
| 타입 검사 | 통과 | `npm run typecheck` |
| 단위 테스트 | 통과 | `npm test`, 59개 통과 (출석 위치 거리·GPS 정확도 검증 포함) |
| 운영 스크립트 문법 검사 | 통과 | `scripts/setup-db.mjs`, `scripts/create-admin.mjs` |
| 프로덕션 빌드 | 통과 | `npm run build`, Vite chunk size 경고만 있음 |
| 헬스 체크 | 통과 | `/api/health`, `/api/ready`, `/api/funnel/status` |
| 운영 readiness | 통과 | Cloud Run `db:"ready"`, `encryption:true` |
| 구역반 API 확인 | 통과 | 한양수자인은 `12구역`, 강남아이파크는 `8구역`, 미매칭 주소는 `99구역` |
| 자원봉사자 구역 저장 확인 | 통과 | `/api/volunteers` 수동 설정 `12구역 12-4반` 저장/응답 확인 |
| 자원봉사 구역 연동 | 통과 | 주소 매핑 시 인적 사항 구역과 구역반 패널이 함께 `12구역`으로 갱신되고 서버 저장값도 정규화됨 |
| 동시 신규 접수 | 통과 | 자원봉사 신청 12건 동시 요청 모두 성공, 접수번호 12건 중복 없음 |
| 동시 수정 충돌 | 통과 | 동일 `updatedAt` 수정 2건 중 1건 성공, 후속 요청은 HTTP 409 반환 |
| 인증번호 경합 | 통과 | 동일 이메일 인증번호 생성 8건 동시 요청에서 unique/partial commit 오류 없음 |
| 봉사 일정 경합 | 통과 | 정원 1명 일정에 동시 신청 2건 결과 `200/409`, 최종 등록 1명 |
| 공개 일정 API | 통과 | 비로그인 조회 가능, 응답에 `createdBy` 등 운영자 식별정보 없음 |
| 운영자 권한 분리 | 통과 | 일반 `admin`의 순례자 건강정보 API 접근은 HTTP 403 |
| 화면 회귀 | 통과 | Q&A 폼 초기 숨김·모달 열기·자동 포커스, 공개 일정 메뉴·카드 확인 |
| 모바일 레이아웃 | 통과 | 390x844에서 Q&A 모달과 일정 카드 가로 넘침 없음 |
| 독립 현장 출석 화면 | 통과 | `/attendance` 비로그인 화면, 1440x900·390x844 가로 넘침 없음 |
| 출석 API 접근 제어 | 통과 | 비로그인 `/api/attendance/checkpoints`, `/api/admin/attendance` 모두 HTTP 401 |
| 경량 부하 테스트 | 통과 | 동시 100, 5초간 32,392건, 실패 0, p95 21.5ms |
| 의존성 보안 감사 | 통과 | `npm audit --audit-level=moderate`, 취약점 0건 |

로컬 `/api/ready`는 개발 환경이라 `encryption:false`, `notifications.email:false`, `notifications.sms:false`로 확인되었습니다. 운영 Cloud Run에서는 `NODE_ENV=production`과 `DATA_ENCRYPTION_KEY`가 적용되어 `encryption:true`가 정상 기준입니다.

## 2-1. 2026-08-17 검토 의견 회귀 결과

| 항목 | 결과 | 비고 |
| --- | --- | --- |
| 타입 검사 | 통과 | `npm run typecheck` |
| 단위 테스트 | 통과 | `npm test`, 68개 통과 |
| 프로덕션 빌드 | 통과 | `npm run build`, 기존 Vite 청크 크기 경고만 있음 |
| DB 마이그레이션 | 통과 | SQLite `submission_source`, `district_targets` 생성 확인 |
| 구역 목표값 검증 | 통과 | 1~12구역 정수 정규화, 음수·소수·배열 입력 전체 거부 |
| 9·10구역 판별 | 통과 | 방죽1마을은 9구역, 방죽마을·밤고개로23길은 10-1반, 밤고개로27길·29길은 10-2반 |
| 관리자 목록 | 통과 | 접수 경로·개인정보 동의 표시/필터 및 엑셀 조건 연동 타입·빌드 검증 |
| 구역 그래프 표시 | 통과 | 목표 가정·신청 가정 막대 비교, 수용 가능 인원 툴팁 표시 빌드 검증 |
| 종이 신청 이력 대조 | 통과 | 감사 이력이 있는 접수 경로 불일치 건만 종이 신청으로 정리하도록 타입·빌드 검증 |

## 3. 회귀 테스트 체크리스트

- `/apply` 신청 유형 카드가 홈스테이와 자원봉사자를 명확히 분리한다.
- `/apply/homestay` 주소 입력 후 자동 구역반이 표시되고 수동 수정할 수 있다.
- 한양수자인 401~426동은 12구역으로 매핑된다.
- 강남아이파크 7단지는 8구역 8-2반으로 매핑된다.
- 편성표에 없는 주소는 99구역 구역외로 매핑된다.
- `/apply/volunteer` 지원 언어는 복수 선택 가능하고 기타 입력은 기타 선택 시에만 열린다.
- `/apply/volunteer` 주소 입력 후 자동 구역반이 표시되고 수동 설정할 수 있다.
- 자원봉사자 접수 조회는 PIN을 요구하지 않는다.
- 홈스테이 개인정보 수집·이용 동의가 없으면 제출을 거부한다.
- 침대 제공 가능 인원은 1명 이상이고 전체 수용 가능 인원을 넘지 않는다.
- 확정 또는 취소 상태의 신청은 신청자가 직접 수정·취소하지 못한다.
- 개인정보 관리자는 종이 신청 임시 PIN 발급과 기존 신청 PIN 재발급을 사용할 수 있다.
- `/admin` 검색은 접수번호뿐 아니라 성명, 연락처, 이메일 통합 검색을 지원한다.
- `/schedule`은 비로그인 상태에서도 일정을 표시하고 신청 시 접수 확인으로 연결한다.
- `/community` 문의 등록 폼은 버튼 클릭 전 숨겨지고 모달로 열린다.
- 개인정보 원본 조회, 수정, 감사 로그 다운로드는 권한 있는 관리자에게만 허용된다.
- `/attendance`는 운영자 콘솔과 분리되어 있고 승인된 운영자 로그인 후에만 지점·스캔 API를 사용한다.
- `/admin#attendance`의 체크 지점 관리와 위치 이력은 개인정보 관리자만 접근한다.

## 4. 추가 수동 검증 권장

카카오 주소검색은 외부 iframe/script에 의존하므로 자동 단위 테스트만으로 충분하지 않습니다. 운영 배포 후 실제 브라우저에서 다음을 확인합니다.

- 카카오 주소검색 팝업 또는 레이어가 열린다.
- 우편번호, 기본 주소, 상세 주소가 분리 입력된다.
- 모바일 화면에서 신청 단계 이동 시 불필요하게 최상단으로 튀지 않는다.
- 관리자 로그인 화면과 관리자 대시보드가 모바일/데스크톱에서 좌우 흰 여백 없이 표시된다.
- 운영 Google Maps API 키를 설정한 뒤 주소 검색 시 위도·경도가 자동 입력되고 지도 핀 보정이 동작한다.
